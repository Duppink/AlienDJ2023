using UnityEngine;

public static class CursorController
{
   /* private static CursorCollection cursorColeccionito;

    private static Vector2 cursorHotspot;


    static CursorController()
    {
        cursorColeccionito = Resources.Load<CursorCollection>("CursorTest");
    }

    public static void SetDefault()
    {
        cursorHotspot = new Vector2(cursorColeccionito.DefaultCursor.width / 2, cursorColeccionito.DefaultCursor.height / 2);
        Cursor.SetCursor(cursorColeccionito.DefaultCursor, cursorHotspot, CursorMode.Auto);
    }

    public static void SetHoover()
    {
        cursorHotspot = new Vector2(cursorColeccionito.OverCursor.width / 2, cursorColeccionito.OverCursor.height / 2);
        Cursor.SetCursor(cursorColeccionito.OverCursor, cursorHotspot, CursorMode.Auto);
        Debug.Log("OnMouseDown wiwi");
    }

    public static void SetPressed()
    {
        cursorHotspot = new Vector2(cursorColeccionito.SelectCursor.width / 2, cursorColeccionito.SelectCursor.height / 2);
        Cursor.SetCursor(cursorColeccionito.SelectCursor, cursorHotspot, CursorMode.Auto);
        Debug.Log("OnMouseOver wowo");
    }

    */
}
